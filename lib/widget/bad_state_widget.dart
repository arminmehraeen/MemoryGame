import 'package:flutter/material.dart';

class BadStateWidget extends StatelessWidget {
  const BadStateWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Center(child: Text("Bad State")) ;
}

